https://github.com/Apollo-Level2-Web-Dev/B5A3

1. Vercel

- Vercel CLI
  -Install

  (First time steps)
  npm install -g vercel
  vercel -v
  vercel login

  (For Every project)
  vercel --prod

-Without Install Process
(For First Time)
npx vercel login
(For every project)
npx vercel --prod
